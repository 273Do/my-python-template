[project]
name = "template"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ruff",
    "lefthook",
    "pytest",
    "pandas",
    "pandera",
    "matplotlib",
]
